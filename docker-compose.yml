version: '3'

networks:
  proxy-tier:

services:
  db:
    image: postgres:alpine
    restart: always
    volumes:
      - ./volumes/db:/var/lib/postgresql
    env_file:
      - db.env


#########
## WEB ##

  web-builder:
    image: jekyll/jekyll
    restart: always
    command: jekyll build --watch --force_polling
    volumes:
      - ./web/html:/srv/jekyll
      - ./volumes/html:/srv/jekyll/_site
    environment:
      - JEKYLL_ENV=production

  web-proxy:
    image: nginx:alpine
    restart: always
    expose:
      - 80
    volumes:
      - ./volumes/html:/usr/share/nginx/html:ro
      - ./web/nginx.conf:/etc/nginx/conf.d/default.conf
    environment:
      - VIRTUAL_HOST=localhost
#     - LETSENCRYPT_HOST=localhost
    depends_on:
      - web-builder
    networks:
      - proxy-tier


#########
## GIT ##

  git:
    image: jonashaag/klaus
    command: >-
      uwsgi --plugin python --http11-socket 0.0.0.0:80
      -w klaus.contrib.wsgi_autoreload --env KLAUS_REPOS=/repos
    expose:
      - 80
    restart: always
    volumes:
      - ./volumes/git:/repos
    environment:
      - VIRTUAL_HOST=git.localhost
#     - LETSENCRYPT_HOST=git.localhost
      - KLAUS_SITE_NAME="Git Sources"
    networks:
      - proxy-tier


###############
## NEXTCLOUD ##

  cloud-app:
    image: nextcloud:fpm-alpine
    restart: always
    volumes:
      - ./volumes/nextcloud:/var/www/html
    env_file:
      - ./cloud/env
    depends_on:
      - db

  cloud-proxy:
    image: nginx:alpine
    restart: always
    volumes:
      - ./volumes/nextcloud:/var/www/html:ro
      - ./cloud/nginx.conf:/etc/nginx/nginx.conf
    environment:
      - VIRTUAL_HOST=cloud.localhost
#     - LETSENCRYPT_HOST=cloud.localhost
    depends_on:
      - cloud-app
    networks:
      - proxy-tier
      - default

#################
## PROXY & SSL ##

  proxy:
    image: jwilder/nginx-proxy:alpine
    restart: always
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./proxy/uploadsize.conf:/etc/nginx/conf.d/uploadsize.conf
      - ./volumes/certs:/etc/nginx/certs:ro
      - ./volumes/vhost.d:/etc/nginx/vhost.d
      - ./volumes/html:/usr/share/nginx/html
      - /var/run/docker.sock:/tmp/docker.sock:ro
    networks:
      - proxy-tier

# ssl:
#   image: jrcs/letsencrypt-nginx-proxy-companion
#   restart: always
#   volumes_from:
#     - proxy
#   volumes:
#     - /var/run/docker.sock:/var/run/docker.sock:ro
#     - ./volumes/certs:/etc/nginx/certs
#   environment:
#     - DEFAULT_EMAIL=root@localhost

  ssl:
    image: paulczar/omgwtfssl
    restart: "no"
    volumes:
      - ./volumes/certs:/certs
    environment:
      - SSL_SUBJECT=localhost
      - CA_SUBJECT=root@localhost
      - SSL_KEY=/certs/localhost.key
      - SSL_CSR=/certs/localhost.csr
      - SSL_CERT=/certs/localhost.crt
    networks:
      - proxy-tier
